<% content_for :body do %>
  <ul id="states">
    <% @states.each do |state| %>
      <li>
        <%= link_to state.name, refinery.states_state_path(state) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
